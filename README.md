# WhileExampleReadFile
Uses a While loop to read a file.  Make sure to add a text file to your project and set the property Build Action to Conten and the Copy to Output Directory property to Copy Always.
